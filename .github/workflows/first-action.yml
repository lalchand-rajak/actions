name: first action

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome
          run: echo "welcome to github actions"
        - name: listing the files
          run: ls